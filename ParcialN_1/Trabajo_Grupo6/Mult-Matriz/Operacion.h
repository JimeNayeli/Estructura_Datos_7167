#include "Matriz.h"
#pragma once
class Operacion{
	public:
	int **segmentar(int,int);
    void encerar(Matriz,int,int);
	void ingresar(Matriz,int,int);
    void imprimir(Matriz, int,int);
	void procesar(Matriz,Matriz,Matriz, int,int,int);

	private:
	
};